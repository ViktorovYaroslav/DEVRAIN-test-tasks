fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1
# Anthropic (Claude)
anthropic==0.34.0

# Ensure httpx version compatible with anthropic's client wrapper (proxies kwarg)
httpx==0.25.2

# Note: RAG dependencies (sentence-transformers, scikit-learn, pandas) will be added later
# to avoid Windows build issues during Task 2.
